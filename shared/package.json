{
  "name": "@music-downloader/shared",
  "version": "1.0.0",
  "description": "Shared TypeScript types for music-downloader",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test:parser": "npm run build && node dist/test-parser.js",
    "examples": "npm run build && node dist/parser-examples.js"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
